public class Main {
    public static void main(String[] args) {
        int[] firstArr = { 1, 2, 3, 0, 0, 0 };
        int[] secArr = { 2, 5, 6 };

        int left = 0;
        int right = 0;
        int firstArrLength = firstArr.length;
        int secArrLength = secArr.length;
        int temp;

        while (left < firstArrLength || right < secArrLength) {
            if(firstArr[left] > secArr[right]){
                
            }
        }

        System.out.println(firstArr);
    }
}